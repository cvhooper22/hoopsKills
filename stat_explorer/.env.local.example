# Copy to .env.local (already gitignored) and fill in real values.
# Used by src/setupProxy.js to reach the image-uploader Lambda
# (see ../image-uploader-lambda) without exposing the API key to the browser.
IMAGE_UPLOAD_API_URL=https://abc123xyz.execute-api.YOUR_REGION.amazonaws.com/prod/images
IMAGE_UPLOAD_API_KEY=
